<%= link_to product_path(product), class: 'product' do %>
    
    <h2> <%= product.title%> </h2>
    
    <span> <%=product.price%>$ </span>

    <span> <%= image_tag product.photo, width: 200 if product.photo.attached? %> </span>
    <% end %>